import { Box, AppBar, Toolbar } from '@mui/material'
import React from 'react'

const NavBar = () => {
return (
    <AppBar>
        <Toolbar>
            
        </Toolbar>
    </AppBar>
)
}

export default NavBar
