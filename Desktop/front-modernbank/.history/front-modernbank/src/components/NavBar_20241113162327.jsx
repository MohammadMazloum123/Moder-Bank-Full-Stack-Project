import { AppBar, Toolbar } from '@mui/material'
import React from 'react'

const NavBar = () => {
return (
    <AppBar position=''>
        <Toolbar>
            NavBar
        </Toolbar>
    </AppBar>
)
}

export default NavBar
